// import PropTypes from 'prop-types';
import { PageStyled } from './PageTitle.module.jsx';

export const PageTitle = ({ title }) => {
  return <PageStyled>{title}</PageStyled>;
};

// PageTitle.PropTypes = {
//   text: PropTypes.string.isRequired,
// };
